# Discarr Configuration
# Copy to .env and fill in values. See README.md and ARCHITECTURE.md for details.

# Output mode. Only screen_share is implemented; others are stubs.
# screen_share | virtual_webcam | hardware_capture
OUTPUT_MODE=screen_share

# Platform. Affects display/device handling. Only linux tested.
# linux | windows
PLATFORM=linux

# API server port
PORT=3000

# Discord - required for screen_share. Enable Developer Mode, then right-click to Copy ID.
# Server ID and voice channel ID where video will be streamed
DISCORD_SERVER_ID=
DISCORD_VOICE_CHANNEL_ID=
# Persists Firefox profile so login survives restarts
DISCORD_PROFILE_PATH=./discord-profile
# Optional: auto-login with credentials (no QR code needed). Leave empty for QR login.
DISCORD_EMAIL=
DISCORD_PASSWORD=

# Jellyfin - optional. Enables play from Jellyfin URLs.
# Server URL (e.g. https://jellyfin.example.com), API key from dashboard, user ID
JELLYFIN_SERVER_URL=
JELLYFIN_API_KEY=
JELLYFIN_USER_ID=

# Base path for local videos. In Docker, bind mount your library here.
VIDEOS_PATH=/videos
